import { ConversationService } from './conversation-service';

describe('ConversationService', () => {
  it('should create an instance', () => {
    expect(new ConversationService()).toBeTruthy();
  });
});
